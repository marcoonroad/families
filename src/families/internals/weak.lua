local export = { }

---------------------------------------------------------------------

export.key   = { __mode = 'k', }
export.value = { __mode = 'v', }
export.pair  = { __mode = 'kv', }

---------------------------------------------------------------------

return export

-- END --
